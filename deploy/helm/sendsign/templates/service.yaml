apiVersion: v1
kind: Service
metadata:
  name: {{ include "sendsign.fullname" . }}
  labels:
    {{- include "sendsign.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "sendsign.selectorLabels" . | nindent 4 }}
