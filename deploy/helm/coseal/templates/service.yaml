apiVersion: v1
kind: Service
metadata:
  name: {{ include "coseal.fullname" . }}
  labels:
    {{- include "coseal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "coseal.selectorLabels" . | nindent 4 }}
