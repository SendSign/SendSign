apiVersion: v1
kind: Service
metadata:
  name: sendsign
  namespace: sendsign
  labels:
    app.kubernetes.io/name: sendsign
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: sendsign
